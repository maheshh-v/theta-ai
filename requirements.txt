# Personal AI Assistant — pinned dependencies (tested versions).
# Python 3.10+ recommended (developed and verified on 3.12).

mcp==2.0.0              # Model Context Protocol SDK (servers + client)
gradio==6.22.0          # Web chat UI
requests==2.34.2        # REST calls to Gemini / Ollama
python-dotenv==1.2.2    # Load GEMINI_API_KEY etc. from a local .env

# Notes:
# - No LLM SDK is required: Gemini and Ollama are called over plain REST.
# - With no API key set, the app runs on a built-in mock LLM (demo mode).
