"""Notion: search, read, create and edit pages and database rows.

Nothing is re-exported here on purpose — import the modules (`api`, `pages`,
`databases`) directly, so a function never shadows the module it lives in.
"""
